/**
 * Created by Tloy on 2018/4/20.
 */

import {StyleSheet, Dimensions, Platform} from 'react-native';
import * as Color from '../configs/Color'
import {width, height, unitWidth, titleHeight, unitHeight} from '../utils/AdapterUtilNew'

export default CoinGiveStyle = StyleSheet.create({
    block: {
        width: width,
        paddingHorizontal: unitWidth * 30,
        backgroundColor: 'white'
    },
    image: {
        width: unitWidth * 50,
        height: unitWidth * 50,
        marginRight: unitWidth * 30,
        resizeMode: 'contain'
    },
    balance: {
        fontSize: unitWidth * 50,
        color: Color.themeColor
    },
    textBlack: {
        fontSize: unitWidth * 30,
        color: "#000000"
    },
    textGary: {
        fontSize: unitWidth * 30,
        color: "#a2a1a1"
    },
    inputItem: {
        width: unitWidth * 690,
        height: unitWidth * 110,
        alignItems: 'center',
        flexDirection: 'row'
    },
    inputLine: {
        width: unitWidth * 690,
        height: unitWidth * 2,
        backgroundColor: Color.gray
    },
    input: {
        flex: 1,
    },
    pasteBt: {
        width: unitWidth * 140,
        height: unitWidth * 50,
        borderRadius: unitWidth * 25,
        backgroundColor: Color.themeColor,
        alignItems: 'center',
        justifyContent: 'center'
    },
    pasteText: {
        fontSize: unitWidth * 24,
        color: "#ffffff"
    },
    clean: {
        width: unitWidth * 50,
        height: unitWidth * 50
    },
    feeRow: {
        width: width,
        flexDirection: 'row',
        justifyContent: "space-between",
        paddingHorizontal:unitWidth*30
    },

    giveAddressInfo: {
        color: "#fd020e",
        textAlign: 'right'
    },
})