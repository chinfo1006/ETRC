/**
 * Created by Tloy on 2018/3/21.
 */

import {StyleSheet, Dimensions, Platform} from 'react-native';
import * as Color from '../configs/Color'
import {width, height, unitWidth, titleHeight} from '../utils/AdapterUtilNew'

export default RiskFundStyle = StyleSheet.create({
    top: {
        width: unitWidth * 750,
        height: unitWidth * 200,
        alignItems: 'center',
        justifyContent: 'center'
    },
    title: {
        fontSize: unitWidth * 36,
        color: Color.white,
        backgroundColor: Color.transparent
    },
    subTitle: {
        fontSize: unitWidth * 48,
        color: Color.white,
        marginTop: unitWidth * 10,
        marginBottom: unitWidth * 10,
        backgroundColor: Color.transparent
    },
    zhanbi: {
        fontSize: unitWidth * 30,
        color: Color.white,
        marginTop: unitWidth * 10,
        marginBottom: unitWidth * 10,
        backgroundColor: Color.transparent
    },
    text: {
        fontSize: unitWidth * 30,
        color: Color.white,
        backgroundColor: Color.transparent
    },
    list: {
        width: width,
        height: height - titleHeight - unitWidth * 200,
    },
})