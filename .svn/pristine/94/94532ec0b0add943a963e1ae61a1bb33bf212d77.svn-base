/**
 *  Created by majunhui on 2017/10/9
 */

import {StyleSheet} from 'react-native'
import {width, height, unitWidth, safeAreaViewHeight} from '../utils/AdapterUtilNew'
import * as Color from '../configs/Color'

export default TabStyle = StyleSheet.create({
    background: {
        alignItems: 'center',
        backgroundColor: 'white',
        borderTopWidth: StyleSheet.hairlineWidth * 2,
        borderColor: '#eeeeee',
        height: unitWidth * 120,
        alignItems: 'center'
    },
    icon: {
        width: unitWidth * 50,
        height: unitWidth * 50,
        resizeMode: 'contain',
        // backgroundColor: 'red'
    },
    titleStyle: {
        color: '#494949',
        fontSize: unitWidth * 24
    },
    titleSelectStyle: {
        color: Color.themeColor,
        fontSize: unitWidth * 24,
        // backgroundColor: 'blue'
    },
    circleBt: {
        width: unitWidth * 100,
        height: unitWidth * 100,
        borderRadius: unitWidth * 50,
        backgroundColor: '#dddddd',
        position: 'absolute',
        bottom: unitWidth * 50 + safeAreaViewHeight,
        left: (width - unitWidth * 100) / 2,
        alignItems: 'center',
        justifyContent: 'center'
        // borderWidth: StyleSheet.hairlineWidth * 2,
        // borderColor: 'blue',

    },
    circleBt2: {
        width: unitWidth * 100 - StyleSheet.hairlineWidth * 2,
        height: unitWidth * 100 - StyleSheet.hairlineWidth * 2,
        borderRadius: (unitWidth * 100 - StyleSheet.hairlineWidth * 2) / 2,
        backgroundColor: 'white',
        alignItems: 'center',
        justifyContent: 'center'
    },
    circleBtbg: {
        width: unitWidth * 106,
        height: unitWidth * 72,
        backgroundColor: 'white',
        position: 'absolute',
        top: unitWidth * 30 + StyleSheet.hairlineWidth * 2,
        left: -unitWidth * 3
    },
    circleBtImage: {
        width: unitWidth * 80,
        height: unitWidth * 80,
        position: 'absolute',
        top: unitWidth * 10 ,
        left: unitWidth * 10 ,
    },
})

