/**
 * Created by Tloy on 2018/1/5.
 */

import {StyleSheet, Platform} from 'react-native';
import * as Color from '../configs/Color';
import {width, height, unitWidth, titleHeight, statusBarHeight} from '../utils/AdapterUtil'

export default MineStyle = StyleSheet.create({
    top: {
        width: width,
        height: unitWidth * 500 + statusBarHeight,
        alignItems: 'center',
    },
    headPic: {
        width: unitWidth * 200,
        height: unitWidth * 200,
        borderRadius: unitWidth * 100,
        borderWidth: unitWidth * 10,
        borderColor: "#fdb985",
        marginTop: unitWidth * 60 + statusBarHeight,
    },

    userName: {
        fontSize: unitWidth * 50,
        color: "white",
        marginTop: unitWidth * 20,
        backgroundColor: 'transparent'
    },
    verify: {
        width: unitWidth * 89,
        height: unitWidth * 72
    },
    recommand: {
        fontSize: unitWidth * 40,
        color: "white",
        marginTop: unitWidth * 30,
        backgroundColor: 'transparent'
    },

    setting: {
        position: 'absolute',
        top: unitWidth * 30 + statusBarHeight,
        right: unitWidth * 40,
    },
    message: {
        position: 'absolute',
        top: unitWidth * 30 + statusBarHeight,
        left: unitWidth * 40,
    },
    newMessage: {
        position: 'absolute',
        top: 0,
        right: 0,
        width: unitWidth * 20,
        height: unitWidth * 20,
        borderRadius: unitWidth * 10,
        backgroundColor: 'white'
    },
    settingIcon: {
        width: unitWidth * 80,
        height: unitWidth * 80,
        resizeMode: 'stretch'
    },

    qqinfo: {
        flexDirection: 'row',
        width: width,
        height: unitWidth * 200,
        backgroundColor: 'white',
        alignItems: 'center',
    },
    qqinfoLine: {
        width: StyleSheet.hairlineWidth * 2,
        height: unitWidth * 100,
        backgroundColor: 'gray'
    },
    qqInfoItem: {
        flex: 1,
        alignItems: 'center',
        justifyContent: 'center',
    },
    redText: {
        color: 'red',
        fontSize: unitWidth * 45
    },
    functionLabel: {
        marginTop: unitWidth * 10
    },

    jifen: {
        width: width,
        height: unitWidth * 250,
        alignItems: 'center',
        justifyContent: 'center'
    },

    jifenText: {
        fontSize: unitWidth * 60,
        color: "white",
        backgroundColor: 'transparent',
    },

    functionLine: {
        width: width,
        height: StyleSheet.hairlineWidth * 2,
        backgroundColor: 'transparent'
    },

    infoBlock: {
        flexDirection: 'column',
        flex: 1
    },
    listItemArrow: {
        width: unitWidth * 28,
        height: unitWidth * 40,
        marginRight: unitWidth * 30,

    },
    functionBlock: {
        flexDirection: 'row',
        alignItems: 'center',
        justifyContent: 'space-between',
        // marginTop: unitWidth * 40,
        width: width,
        height: unitWidth * 200,
        backgroundColor: 'white',
    },
    functionIcon: {
        width: unitWidth * 100,
        height: unitWidth * 100,
        resizeMode: 'contain'
    },
    functionTitle: {
        color: 'black',
        fontSize: unitWidth * 40
    },
    itemLine: {
        width: width,
        height: StyleSheet.hairlineWidth * 2,
        backgroundColor: Color.gray,
        marginLeft: unitWidth * 120
    },

    myShopInfo: {
        width: width,
        backgroundColor: 'white',
        alignItems: 'center',
        paddingTop: unitWidth * 30,
        paddingBottom: unitWidth * 60
    },
    myShopInfoManage: {
        position: 'absolute',
        bottom: unitWidth * 10,
        right: unitWidth * 100,
        backgroundColor: 'transparent'
    },

    //设置字体颜色 
    infoText: {
        color: 'white',
    }
})